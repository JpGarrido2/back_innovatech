//nota
//cont express_graphql = require('express-graphql');
//cont {buildShema} = require('graphql'); 
//cont schema = buildShema();

//conr root = {
/*  message() => "hola "
}

app.use('/graphql', express_graphql({
  schema:schema,
  root: root,
  graphql:true
}))

//practica de jessica 
app.listen(3000,()=>console.log('server on port 3000'));*/
